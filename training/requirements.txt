# Deliberately pinned baseline stack; not a claim to use the latest release.
transformers==4.57.6
peft==0.18.1
accelerate==1.12.0
bitsandbytes==0.49.2
# Torch is installed separately from the official CUDA 12.8 wheel index.
