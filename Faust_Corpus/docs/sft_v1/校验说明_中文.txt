第二步质量核验

已交付195条说明—原文对。第一部80条，第二部115条。训练、验证、测试分别为158、18、19条；与第一步逐项匹配。原包42份文件的内容校验值全部保持一致，没有为了加入说明而重新切分、改字或调换集合。

第二步自动检查共57项，全部通过。覆盖ID唯一性、原顺序、说明非空、目标与原文一致、来源事件完整、结构化字段对应、prompt仅含允许字段、中文仅进入阅读材料、集合文件与主记录一致、整场景组互斥、索引对应与哈希。第一步原文审计另行重跑，61项也全部通过；该报告只说明原著底稿仍完好，不代替本次说明的语义核查。

全部195段均已由助手阅读、制作说明并完成第二遍自查，处理转述、愿望、人物宣称、梦境与实际行动的差别，未留给用户一批待完成的德语摘要。这里的自查不是外部专家或独立模型的背书，也不能把文学解释的不确定性归零。

将每个德语输入与全部195段目标逐词比较，忽略大小写与标点后，9个输入出现普通四词匹配，最大连续匹配为4词；没有5词及以上连续匹配。已查看匹配，保留必要的人名、普通动作与常用语法，不把“四词重合”机械等同抄袭。检查不针对内容相似性：输入本就必须概括目标内容；它也不能证明底模预训练未见过原文。

德语输入按本报告的Unicode分词方法计数为85至156词，中位123词。这不是模型token数。原文目标使用第一步的不变字符串；本阶段没有进行特定模型的模板渲染或token计数。

含195条的faust_sft_pairs.jsonl用于审计和管理。训练仅用splits/sft_v1/train.jsonl。中文阅读版不参加训练，不会把中文说明混进德语提示。
